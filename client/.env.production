# .env.production — used by Vercel when building for production
# DO NOT add local URLs here. These point to the live deployed services.

VITE_API_BASE_URL=https://interviewpilot-backend-cc5e.onrender.com/api/v1
